# OAuth Testing

This repository is for OAuth testing. It contains test implementations of an OAuth and OpenID Connect Client, Authorisation Server, and Resource Server.

## Project directories

- `client/`
- `authorisation-server/`
- `resource-server/`
