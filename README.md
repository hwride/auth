# Authentication Testing

This repository is for authentication testing.

## OAuth Client

A simple OAuth client is implemented under `/oauth`.

Note this is not for use in production, and is just for testing.

### Run

1. Set required environment variables:
   - `AUTH_SERVER_BASE`
   - `CLIENT_ID`
   - `CLIENT_SECRET`
2. Start the development server:
   - `npm run dev`
